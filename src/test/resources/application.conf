akka {
  log-dead-letters = off
  test {
    single-expect-default = 200
  }
  persistence {
    journal.plugin = akka.persistence.journal.inmem
    snapshot-store.plugin = akka.persistence.snapshot-store.local
  }
}
